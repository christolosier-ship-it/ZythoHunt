export function createBubbleSprite({ size = 60, dpr = 1, family = 'pale-ale-ipa', state = 'unknown' } = {}) { const c = document.createElement('canvas'); const px = Math.ceil((size * 2 + 24) * dpr); c.width = px; c.height = px; const ctx = c.getContext('2d'); if (!ctx) return c; ctx.scale(dpr, dpr); const r = size; const cx = px / dpr / 2; const cy = cx; const colors = { wheat: '#d9c26a', lager: '#e2b62d', spontaneous: '#c96b52', 'mixed-wild': '#8c5873', 'pale-ale-ipa': '#d88718', core: '#9d7842' }; ctx.shadowColor = state === 'selected' ? '#fff5c9' : colors[family] || colors.core; ctx.shadowBlur = state === 'unknown' ? 8 : 16; ctx.fillStyle = state === 'unknown' ? '#6f5129aa' : colors[family] || colors.core; ctx.beginPath(); ctx.arc(cx, cy, r, 0, Math.PI * 2); ctx.fill(); ctx.strokeStyle = '#fff8'; ctx.lineWidth = 3; ctx.stroke(); ctx.fillStyle = '#fff7'; ctx.beginPath(); ctx.ellipse(cx - r * .25, cy - r * .35, r * .22, r * .1, -0.35, 0, Math.PI * 2); ctx.fill(); return c; }
