export function wrapCanvasText(ctx, text, maxWidth, maxLines = 3) { const words = String(text || '').split(/\s+/).filter(Boolean); const lines = []; let line = ''; for (const word of words) { const next = `${line} ${word}`.trim(); if (ctx.measureText(next).width > maxWidth && line) { lines.push(line); line = word; } else line = next; } if (line) lines.push(line); if (lines.length <= maxLines) return lines; const kept = lines.slice(0, maxLines); kept[maxLines - 1] = `${kept[maxLines - 1].replace(/…$/, '')}…`; return kept; }
export function drawCenteredLabel(ctx, text, x, y, radius, options = {}) { const question = text === '?'; ctx.save(); ctx.fillStyle = options.fillStyle || (question ? '#fff5c9' : '#fff7e5'); ctx.textAlign = 'center'; ctx.textBaseline = 'middle'; ctx.font = `800 ${question ? 42 : Math.max(13, Math.min(20, radius / 3))}px ui-rounded, system-ui, sans-serif`; const lines = question ? ['?'] : wrapCanvasText(ctx, text, radius * 1.45, 3); const lineHeight = question ? 42 : 18; lines.forEach((line, i) => ctx.fillText(line, x, y + (i - (lines.length - 1) / 2) * lineHeight)); ctx.restore(); return lines; }
