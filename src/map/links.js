export function renderLinks(group, links, nodeById) { group.replaceChildren(); for(const l of links){ const a=nodeById.get(l.sourceId), b=nodeById.get(l.targetId); if(!a||!b) continue; const line=document.createElementNS('http://www.w3.org/2000/svg','line'); line.setAttribute('x1',a.position.x); line.setAttribute('y1',a.position.y); line.setAttribute('x2',b.position.x); line.setAttribute('y2',b.position.y); line.setAttribute('class',`map-link ${l.linkType==='primary'?'is-primary':'is-secondary'}`); line.dataset.linkId=l.id; group.append(line); } }
