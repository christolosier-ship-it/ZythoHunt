export const VISUAL_STATE_PRIORITY = ['unknown', 'reveal-pending', 'selected', 'explored', 'discovered'];
export function createPresentationState(overrides = {}) { return { revealPendingId: null, selectedId: null, exploredIds: new Set(), reducedMotion: false, isInteracting: false, ...overrides, exploredIds: overrides.exploredIds instanceof Set ? overrides.exploredIds : new Set(overrides.exploredIds ?? []) }; }
export function resolveNodeVisualState(node, presentationState = {}, discoveredIds = new Set()) { if (node.functionalType === 'structure') return 'structure'; if (presentationState.revealPendingId === node.id) return 'reveal-pending'; if (!discoveredIds?.has?.(node.id)) return 'unknown'; if (presentationState.selectedId === node.id) return 'selected'; if (presentationState.exploredIds?.has?.(node.id)) return 'explored'; return 'discovered'; }
export function canSelectNode(node, discoveredIds) { return node?.functionalType === 'capturable' && discoveredIds?.has?.(node.id); }
