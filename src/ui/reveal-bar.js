import { buildSearchIndex, findDebugStyleMatches } from '../domain/search.js';
export function createRevealBar(container, nodes, aliases, handlers) { const index = buildSearchIndex(nodes, aliases); const form=document.createElement('form'); form.className='reveal-bar'; form.innerHTML='<label class="sr-only" for="style-reveal-input">Style de bière goûté</label><div class="reveal-row"><input id="style-reveal-input" name="style" autocomplete="off" autocapitalize="words" enterkeyhint="search" placeholder="Saisir un style goûté…"><button type="submit">Révéler</button></div><p class="reveal-message" role="status" aria-live="polite"></p>'; const input=form.querySelector('input'); const msg=form.querySelector('.reveal-message'); const setMessage=t=>{ msg.textContent=t; msg.classList.toggle('is-empty', !t); }; setMessage(''); form.addEventListener('submit', e=>{ e.preventDefault(); const r=findDebugStyleMatches(input.value,index); if(r.status==='empty') return setMessage('Saisissez un style indiqué sur l’étiquette.'); if(r.status==='none') return setMessage('Aucun style ne correspond.'); if(r.status==='ambiguous') return setMessage('Plusieurs styles peuvent correspondre. Précisez le nom.'); const node=r.matches[0]; input.blur(); if(handlers.isDiscovered(node.id)) { handlers.onAlready(node.id); setMessage('Ce style est déjà révélé.'); } else { handlers.onReveal(node.id); setMessage(`${node.name} révélée.`); } }); container.append(form); return { form, input, setMessage }; }
