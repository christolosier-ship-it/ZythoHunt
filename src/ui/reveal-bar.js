import { buildSearchIndex, findDebugStyleMatches } from '../domain/search.js';
export function createRevealBar(container, nodes, aliases, handlers) { const index = buildSearchIndex(nodes, aliases); const form=document.createElement('form'); form.className='reveal-bar'; form.innerHTML='<label><span>Quel style venez-vous de goûter ?</span><input name="style" autocomplete="off" placeholder="West Coast IPA"></label><button>Révéler</button><p class="reveal-message" role="status"></p><small>Prototype : découvertes en mémoire, réinitialisées au rechargement.</small>'; const input=form.querySelector('input'); const msg=form.querySelector('.reveal-message'); form.addEventListener('submit', e=>{ e.preventDefault(); const r=findDebugStyleMatches(input.value,index); if(r.status==='empty') msg.textContent='Saisissez un style indiqué sur l’étiquette.'; if(r.status==='none') msg.textContent='Aucun style du prototype ne correspond à cette saisie.'; if(r.status==='ambiguous') msg.textContent='Plusieurs styles peuvent correspondre. Précisez le nom indiqué sur l’étiquette.'; if(r.status==='single') { input.blur(); const node=r.matches[0]; if(handlers.isDiscovered(node.id)) { handlers.onAlready(node.id); msg.textContent='Ce style est déjà révélé.'; } else { handlers.onReveal(node.id); msg.textContent=`${node.name} révélé.`; } } }); container.append(form); return { form, input, setMessage: t => msg.textContent=t }; }
