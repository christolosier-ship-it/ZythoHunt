export function createToast(root = document.body) { const el = document.createElement('p'); el.className = 'app-toast'; el.setAttribute('role','status'); root.append(el); let timer; return { show(message) { el.textContent = message; el.classList.add('is-visible'); clearTimeout(timer); timer = setTimeout(()=>el.classList.remove('is-visible'), 2600); } }; }
