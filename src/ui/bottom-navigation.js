export function createBottomNavigation(container, toast) { const nav = document.createElement('nav'); nav.className = 'bottom-navigation'; nav.setAttribute('aria-label','Navigation principale'); const items = [['Zythosphère','page'],['Brassopédie','future'],['Dégustations','future'],['Progression','future']]; for (const [label,type] of items) { const b=document.createElement('button'); b.className='bottom-nav-item'; b.innerHTML='<svg aria-hidden="true" viewBox="0 0 24 24"><circle cx="12" cy="12" r="7"></circle></svg><span>'+label+'</span>'; b.setAttribute('aria-label', label); if(type==='page') b.setAttribute('aria-current','page'); else b.onclick=()=>toast.show(`${label} disponible dans une prochaine version.`); nav.append(b); } container.append(nav); return nav; }
