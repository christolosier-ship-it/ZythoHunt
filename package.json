{
  "name": "zythohunt",
  "version": "1.0.0",
  "private": true,
  "description": "PWA d'exploration brassicole, de collection et de dégustation.",
  "type": "module",
  "scripts": {
    "brand-assets:generate": "python scripts/generate-brand-assets.py",
    "content-index:generate": "pnpm search-index:generate && pnpm sensory-catalog:validate",
    "predev": "pnpm brand-assets:generate && pnpm content-index:generate",
    "dev": "vite",
    "prebuild": "pnpm brand-assets:generate && pnpm content-index:generate",
    "build": "vite build && node scripts/prepare-service-worker.mjs",
    "preview": "vite preview",
    "search-index:generate": "node scripts/generate-beer-search-index.mjs",
    "sensory-catalog:validate": "node scripts/validate-sensory-catalog.mjs",
    "assets:thumbs": "python scripts/generate-thumbnails.py",
    "assets:thumbs:check": "python scripts/generate-thumbnails.py --check",
    "typecheck": "tsc --noEmit",
    "typecheck:strict": "tsc --noEmit -p tsconfig.strict.json",
    "test": "pnpm test:unit",
    "test:unit": "node --test",
    "test:e2e": "playwright test",
    "precheck": "pnpm content-index:generate",
    "check": "pnpm typecheck && pnpm typecheck:strict && pnpm test:unit && pnpm build"
  },
  "dependencies": {
    "@fontsource/cinzel": "5.3.0",
    "@fontsource/crimson-text": "5.3.0",
    "gsap": "3.15.0"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1",
    "@types/node": "25.6.2",
    "axe-core": "4.12.1",
    "typescript": "5.9.3",
    "vite": "7.3.3"
  },
  "engines": {
    "node": "24.x"
  },
  "packageManager": "pnpm@10.12.4"
}
